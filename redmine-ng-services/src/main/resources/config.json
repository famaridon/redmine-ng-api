{
  "redmine": {
    "readonlyApiKey": "${env:REDMINE_RO_API_KEY}"
  },
  "http": {
    "cors": {
      "allowedOrigins": [
        "http://localhost:4200",
        "http://localhost:3000"
      ]
    }
  }
}